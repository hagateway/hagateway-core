// build.js
import { build } from 'esbuild';

build({
  entryPoints: ['src/index.ts'],
  bundle: true,
  outfile: 'dist/bundle.js',
  platform: 'node',
  target: 'esnext',
  sourcemap: true,
  minify: false,
//   tsconfig: undefined, // explicitly ignore tsconfig.json
});
