import { defineConfig } from "vite";
import ViteReactPlugin from "@vitejs/plugin-react";


// TODO
export default defineConfig({
    base: "/assets/",
    plugins: [
        ViteReactPlugin(),
    ],
    build: {
        outDir: "dist",
        // emptyOutDir: true,
        rollupOptions: {
            input: "lib/view/index.ejs.html",
        },
    },
});